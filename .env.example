# API base URL for the backend (no trailing slash).
# - Local: leave unset or set to http://localhost:5000 (default)
# - Production (Vercel): set to your deployed backend URL, e.g. https://your-api.railway.app or https://api.horizonbeetech.com
# VITE_API_URL=http://localhost:5000
